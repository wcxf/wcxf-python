language: python
python:
  - "3.5"
  - "3.6"

notifications:
  email: false

install:
  - pip install wcxf
  - pip show wcxf
  - pip install coveralls

script: nosetests --with-coverage --cover-package=wcxf

after_success: coveralls
